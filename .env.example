# Variáveis de Ambiente - KanbanFlow Pro

# ===========================================
# BACKEND (Server)
# ===========================================

# Porta do servidor backend
PORT=3001

# Ambiente de execução
NODE_ENV=development

# URL do frontend (para configuração de CORS)
FRONTEND_URL=http://localhost:5173

# Caminho para o arquivo de dados
DATA_FILE_PATH=./server/data/tasks.json

# ===========================================
# FRONTEND (React + Vite)
# ===========================================

# URL da API backend
VITE_API_URL=http://localhost:3001

# ===========================================
# PRODUÇÃO
# ===========================================

# Para produção, altere os valores acima para:
# NODE_ENV=production
# PORT=3001 (ou porta do seu servidor)
# FRONTEND_URL=https://seu-dominio.com
# VITE_API_URL=https://api.seu-dominio.com
# DATA_FILE_PATH=/caminho/absoluto/para/tasks.json

# ===========================================
# OBSERVAÇÕES IMPORTANTES
# ===========================================

# 1. Nunca commite o arquivo .env no Git
# 2. Adicione .env ao .gitignore
# 3. Configure estas variáveis no seu servidor de produção
# 4. Para Vercel/Netlify, configure nas settings do projeto
# 5. Para Docker, use docker-compose.yml ou --env-file
# 6. Para PM2, use ecosystem.config.js
